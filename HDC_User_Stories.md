# HDC 软件开发 User Story

## 1. 文档说明

- 需求来源：`SGMW_VMC HDC_陡坡缓降功能规范_20260623_V5.0_0630疑问反馈.docx`
- 目标：将 HDC 功能规范整理为面向软件开发、可拆解、可验收的 User Story。
- 适用范围：VMC 侧 HDC 制动控制、悬架协同控制、HMI 状态反馈、故障降级、接口与诊断。
- 说明：原始规范中存在较多 TBD 项，本文保留为“待标定/待确认”，不替代正式系统需求冻结值。

## 2. Epic 划分

| Epic ID | Epic 名称 | 说明 |
|---|---|---|
| EPIC-HDC-01 | 功能开关与状态管理 | HDC 的开关、待命、激活、退出、关闭、故障状态管理 |
| EPIC-HDC-02 | 下坡控速与驾驶员接管 | 目标车速、制动控制、油门/制动干预、退出与恢复 |
| EPIC-HDC-03 | 悬架协同控制 | 陡坡调平、陡坡调高与悬架状态管理 |
| EPIC-HDC-04 | HMI 与状态可视化 | 指示灯、状态显示、目标车速、故障提示、制动灯 |
| EPIC-HDC-05 | 故障降级与安全策略 | 故障检测、降级、恢复、执行器可用性判断 |
| EPIC-HDC-06 | 接口、标定与诊断支撑 | 总线接口、状态量输出、参数化、测试与诊断支撑 |

## 3. Functional User Stories

### US-HDC-001 功能开关控制

**作为** 驾驶员  
**我希望** 通过 HMI/按键主动打开或关闭 HDC 功能  
**以便** 我可以在下坡场景下按需启用或退出 HDC。

**验收标准**

1. Given 车辆处于 `ready` 状态，When 驾驶员打开 HDC 开关且信号有效，Then HDC 应从 `Off` 进入 `Standby`，前提是车速、故障、高优先级功能等条件满足。
2. Given HDC 处于 `Standby`、`Active` 或 `Failure`，When 驾驶员关闭 HDC 开关且信号有效，Then HDC 应进入 `Off`。
3. Given 车辆不满足进入 `Standby` 的条件，When 驾驶员打开 HDC 开关，Then HDC 不应误进入 `Standby` 或 `Active`。
4. Given HDC 开关信号无效或超时，Then 软件应按失效信号策略处理，禁止因异常开关值导致误激活。

### US-HDC-002 待命状态管理

**作为** VMC HDC 控制软件  
**我希望** 在功能已打开但尚未满足激活条件时保持 `Standby` 状态  
**以便** 功能处于可随时激活的准备状态。

**验收标准**

1. Given HDC 开关已打开，When 车辆 `ready`、车速低于上限、ADAS 未激活且无系统故障，Then HDC 应进入 `Standby`。
2. Given HDC 处于 `Standby`，When 任一 `Off` 条件成立，例如车辆非 `ready` 或开关关闭，Then HDC 应退出到 `Off`。
3. Given HDC 处于 `Standby`，Then 软件应对外输出明确状态值，供 HMI、测试和日志使用。

### US-HDC-003 激活条件判定

**作为** 驾驶员  
**我希望** 在满足下坡和安全条件时，HDC 自动进入控速状态  
**以便** 无需持续踩制动也能匀速下坡。

**验收标准**

1. Given HDC 处于 `Standby`，When 车速在允许工作区间、油门释放、驾驶员制动需求低于阈值、档位非 `P`、EPB 已释放、HHC 未保持、ADAS 未激活且坡度条件满足，Then HDC 应进入 `Active`。
2. Given 任一高优先级功能激活，例如 ADAS、ECD、蠕行或内部高优先级模式，Then HDC 不应进入 `Active`。
3. Given 执行器故障或 HDC 系统降级存在，Then HDC 不应进入 `Active`。

### US-HDC-004 初始目标车速建立

**作为** 驾驶员  
**我希望** HDC 在激活时自动记住当前车速作为目标车速  
**以便** 车辆能够从当前下坡速度平顺接管。

**验收标准**

1. Given HDC 从 `Standby` 进入 `Active`，Then 当前实际车速应被记录为初始目标车速。
2. Given 当前车速超出允许目标范围，Then 初始目标车速应被限幅到允许边界。
3. Given HDC 因驾驶员干预退出后再次满足激活条件，Then 软件应使用新的实际车速重新建立目标车速。

### US-HDC-005 下坡控速执行

**作为** 驾驶员  
**我希望** HDC 根据目标车速自动控制机械制动与电制动  
**以便** 车辆在坡道上稳定、平顺地下坡。

**验收标准**

1. Given HDC 处于 `Active`，Then 软件应根据目标车速与实际车速计算目标减速度。
2. Given 目标减速度已计算，Then 软件应输出制动请求到液压制动与电制动执行器。
3. Given 电制动能力可用，Then 软件应优先使用电制动，液压制动用于补偿不足部分。
4. Given 电制动能力受限或 SOC 过高，Then 软件应按策略切换或提高液压制动占比，且应具备滞回避免频繁切换。
5. Given HDC 持续控速，Then 控制过程应避免明显冲击、无非预期的频繁状态跳变。

### US-HDC-006 驾驶员油门接管

**作为** 驾驶员  
**我希望** 通过踩下油门临时接管车辆纵向控制  
**以便** 在需要加速时 HDC 不与我的意图冲突。

**验收标准**

1. Given HDC 处于 `Active`，When 检测到油门开度大于退出阈值，Then HDC 应退出到 `Standby`。
2. Given HDC 因油门退出到 `Standby`，Then 车辆纵向加速应由 VCU/驾驶员输入主导。
3. Given 驾驶员释放油门且重新满足激活条件，Then HDC 可重新进入 `Active`，并以新的实际车速作为目标车速。

### US-HDC-007 驾驶员制动接管

**作为** 驾驶员  
**我希望** 通过踩下制动踏板覆盖 HDC 制动请求  
**以便** 在需要更强减速时优先响应我的制动意图。

**验收标准**

1. Given HDC 处于 `Active`，When 驾驶员总制动需求超过 HDC 请求并超过阈值持续一定时间，Then HDC 应退出到 `Standby`。
2. Given 驾驶员制动接管期间，Then 制动执行器应优先响应驾驶员制动请求。
3. Given 驾驶员释放制动且重新满足激活条件，Then HDC 可重新进入 `Active`，并以新的实际车速作为目标车速。

### US-HDC-008 激活退出与回退

**作为** VMC HDC 控制软件  
**我希望** 对 `Active -> Standby` 退出条件进行统一管理  
**以便** 功能能够在边界条件或冲突场景下安全退出。

**验收标准**

1. Given HDC 处于 `Active`，When 车速超出允许范围，Then HDC 应退出到 `Standby`。
2. Given HDC 处于 `Active`，When 车辆长时间处于平路且 HDC 请求扭矩低于阈值，Then HDC 应退出到 `Standby`。
3. Given HDC 处于 `Active`，When EPB 非释放、HHC 请求停车、ADAS 激活、蠕行激活或内部高优先级功能激活，Then HDC 应退出到 `Standby`。
4. Given HDC 退出到 `Standby`，Then 软件应释放控制请求并同步更新对外状态量。

### US-HDC-009 制动与电制动仲裁

**作为** 系统集成工程师  
**我希望** HDC 的液压制动与电制动请求有确定的仲裁逻辑  
**以便** 执行器之间无冲突且控制意图可验证。

**验收标准**

1. Given HDC 处于 `Active`，Then 软件应同时考虑液压制动与电制动能力上限。
2. Given 驾驶员制动请求存在，Then HDC 请求与驾驶员请求的优先级和取值原则应一致且可追踪。
3. Given VCU 或制动执行器返回不可控状态，Then 软件应按故障或降级策略处理，不应持续输出无效请求。

### US-HDC-010 悬架待命控制

**作为** VMC 悬架控制软件  
**我希望** 在 HDC 开启且悬架功能无故障时进入悬架 `Standby`  
**以便** 在陡坡场景满足条件时可快速进入悬架协同控制。

**验收标准**

1. Given HDC 开关打开且信号有效，When 悬架执行器无故障降级，Then HDC 悬架状态应进入 `Standby`。
2. Given 悬架开关关闭或相关执行器故障降级，Then HDC 悬架状态应进入 `Off` 或 `Failure`。

### US-HDC-011 陡坡调平控制

**作为** 驾驶员  
**我希望** 在较大下坡坡度时车辆自动减小前倾姿态  
**以便** 提升稳定性与驾驶舒适感。

**验收标准**

1. Given HDC 悬架功能处于 `Standby`，When 车速处于允许范围且坡度高于调平门限，Then 悬架状态应进入 `Pitch Leveling Active`。
2. Given 调平功能激活，Then 软件应以后轴高度为参考，对前轴提出调平请求，并协调主动悬架与空簧执行器。
3. Given 坡度回落、车速过高或车辆长时间静止，Then 调平功能应退出到 `Standby`。

### US-HDC-012 陡坡调高控制

**作为** 驾驶员  
**我希望** 在超大下坡坡度时车辆自动提升底盘高度  
**以便** 降低坡底剐蹭风险。

**验收标准**

1. Given HDC 悬架功能处于 `Standby`，When 车速处于允许范围且坡度高于调高门限，Then 悬架状态应进入 `Height Boost Active`。
2. Given 调高功能激活，Then 软件应向前后轴输出一致的调高请求，并允许主动悬架与空簧协同工作。
3. Given 坡度低于退出阈值持续一定时间、车速过高或车辆静止，Then 调高功能应退出到 `Standby`。

### US-HDC-013 调平与调高仲裁

**作为** 悬架控制软件开发工程师  
**我希望** 调平与调高请求能够叠加或仲裁输出  
**以便** 在极端坡道场景下既能降低前倾又能提升离地间隙。

**验收标准**

1. Given 调平和调高条件同时满足，Then 软件应按定义策略叠加或仲裁请求。
2. Given 主动悬架仅能短时支撑高度控制，Then 软件应通过空簧承担稳定高度保持。
3. Given 空簧未配置或不可用，Then 悬架协同功能应按配置策略禁止激活或进入降级。

### US-HDC-014 HMI 状态显示

**作为** 驾驶员  
**我希望** 通过仪表或 HMI 明确看到 HDC 当前状态  
**以便** 我知道功能是否开启、待命、激活或故障。

**验收标准**

1. Given HDC 处于 `Off`，Then HMI 指示应熄灭或显示关闭状态。
2. Given HDC 处于 `Standby`，Then HMI 应显示待命状态，例如常亮指示。
3. Given HDC 处于 `Active`，Then HMI 应显示激活状态，例如闪烁指示或文字提醒。
4. Given HDC 处于 `Failure`，Then HMI 应显示故障或不可用状态，具体样式由项目 HMI 规范定义。

### US-HDC-015 目标车速与尾灯反馈

**作为** 驾驶员  
**我希望** 在 HDC 工作时看到必要的状态反馈，并让后车感知车辆制动行为  
**以便** 提升功能可理解性和道路安全性。

**验收标准**

1. Given HDC 处于 `Active`，Then 软件应支持向 HMI 发送目标车速用于显示。
2. Given HDC 发出制动控制请求，Then 系统应按整车策略触发制动尾灯点亮。
3. Given 尾灯点亮策略由其他控制器决定，Then HDC 应至少输出可供仲裁的状态或请求信号。

### US-HDC-016 故障检测与失败状态

**作为** 安全与诊断工程师  
**我希望** HDC 能够检测关键输入、执行器和通信故障  
**以便** 功能在异常场景下进入安全状态。

**验收标准**

1. Given 关键输入信号无效、超时或不可信，Then HDC 应禁止激活或退出控制。
2. Given 制动执行器、电制动执行器或悬架执行器故障降级，Then HDC 应进入 `Failure`、`Standby` 或 `Off`，具体取决于故障策略。
3. Given HDC 从 `Standby` 或 `Active` 进入 `Failure`，Then 软件应对外发布失败状态并停止不安全控制请求。
4. Given 故障恢复且重新满足条件，Then HDC 应按策略决定是否允许恢复到 `Standby`。

### US-HDC-017 高优先级功能冲突管理

**作为** 系统集成工程师  
**我希望** HDC 与 ADAS、HHC、EPB、蠕行及其他高优先级功能有清晰冲突处理规则  
**以便** 不同纵向控制功能之间不会相互抢占造成异常行为。

**验收标准**

1. Given ADAS、AEB、ACC、APA、AES 或项目定义的高优先级功能激活，Then HDC 不应进入 `Active`，或应立即退出到 `Standby`。
2. Given EPB 未释放、HHC 请求停车、ECD 激活、蠕行激活或车辆速度低于蠕行阈值，Then HDC 应按策略禁止激活或退出。
3. Given 冲突条件解除，Then HDC 仅在重新满足全部激活条件后方可再次进入 `Active`。

## 4. Enabler Stories

### US-HDC-018 对外接口状态输出

**作为** 软件集成与测试工程师  
**我希望** HDC 向外输出标准化的状态、请求和故障信息  
**以便** HMI、执行器、日志系统和测试工具能够一致解析。

**验收标准**

1. 软件应输出 HDC 制动状态，例如 `Off / Standby / Active / Failure`。
2. 软件应输出 HDC 悬架状态，例如 `Off / Standby / Pitch Leveling Active / Height Boost Active / Failure`。
3. 软件应输出目标车速、制动请求、悬架请求及必要的有效位/状态位。
4. 所有对外状态应具备一致的枚举定义、超时策略和无效值处理规则。

### US-HDC-019 标定参数化管理

**作为** 标定工程师  
**我希望** HDC 的关键阈值通过参数配置管理  
**以便** 项目可以在不改代码主流程的情况下完成不同车型或版本的调校。

**验收标准**

1. 软件应支持对工作车速上下限、坡度门限、平路退出门限、油门退出阈值、制动接管阈值、静止判断阈值等进行参数化。
2. 参数应具备默认值、有效范围和越界保护。
3. 参数变更不应破坏状态机一致性和故障处理逻辑。

### US-HDC-020 诊断与可追踪性支撑

**作为** 测试与诊断工程师  
**我希望** HDC 提供可追踪的状态迁移与故障记录  
**以便** 快速定位误激活、误退出和执行器协同问题。

**验收标准**

1. 软件应支持记录关键状态迁移事件，例如 `Off -> Standby`、`Standby -> Active`、`Active -> Standby`、`Any -> Failure`。
2. 软件应支持记录关键退出原因或故障原因，至少能够区分油门接管、制动接管、超速、平路退出、故障降级和高优先级功能冲突。
3. 诊断与日志信息应能支持 DV、SIL/HIL 和整车问题复现。

## 5. 待确认项

以下内容在正式系统需求冻结前应补齐，否则只适合作为开发初稿输入：

1. HDC 最低工作车速、最高工作车速。
2. 油门退出阈值、制动接管阈值、平路退出阈值和去抖时间。
3. 坡度判定门限、坡度变化率条件、调平/调高进入与退出阈值。
4. SOC 高阈值、液压/电制动切换滞回。
5. 制动尾灯最终仲裁责任方。
6. HMI 激活、待命、故障显示样式与文字提示。
7. 悬架功能是否要求空簧为必备配置、主动悬架的短时保持能力边界。

## 6. 建议的开发拆分方式

1. 先冻结状态机与优先级矩阵，再进入编码。
2. 将制动 HDC 与悬架 HDC 拆成两个独立软件特性包，但共享开关、坡度判定和 HMI 状态。
3. 在系统需求层补齐所有 TBD 阈值后，再生成详细软件需求和测试用例。
4. 建议基于本文继续派生 `Software Requirement`、`State Machine Table` 和 `DV Test Case` 三份下游文档。